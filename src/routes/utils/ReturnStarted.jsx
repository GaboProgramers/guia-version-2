import { Link } from 'react-router-dom';

const ReturnStarted = () => {
    return (
        <div className="btn-retur">
            <Link to="/00-started">Return Started</Link>
        </div>
    );
};

export default ReturnStarted;
